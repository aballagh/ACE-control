server:
  port: 8081

spring:
  application:
    name: service-client
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://mysql:3306/aballaghclient?createDatabaseIfNotExist=true
    username: "root"
    password: ""

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
eureka:
  instance:
    hostname: localhost
