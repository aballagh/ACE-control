server:
  port: 8082
spring:
  application:
    name: voiture-service
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/aballaghvoiture?createDatabaseIfNotExist=true
    username: "root"
    password: ""

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
eureka:
  instance:
    hostname: localhost